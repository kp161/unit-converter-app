import 'package:flutter/material.dart';

class ConvertIcon extends StatelessWidget {
  const ConvertIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.swap_horiz_rounded,
      color: Color(0xFF5048E5),
      size: 45);
  }
}
